<!DOCTYPE html>
<html>
  <head>
    <title>Rage Manager :: <%= controller.controller_name.capitalize %></title>
    <%= stylesheet_link_tag "application" %>
    <%= javascript_include_tag :defaults %>
    <%= csrf_meta_tag %>
    <%= yield(:head) %>
  </head>           
   <body> 
   <div id="container">      
      <div id="banner">
        <%= link_to(image_tag("rage.jpg", :size => "450x100", :border => 0), :root) %>
      </div>
      <!-- NAVIGATION BAR --> 
      <div id="nav">
     	  <ul>  
       	  <li><%= link_to 'Parties', :party_path %></li>  
  			  <li><%= link_to 'Guests', :guest_path %></li>
          <li><%= link_to 'Locations', :location_path %></li>
          <li><%= link_to 'Invitations', :invitation_path %></li>
          <li><%= link_to 'Gifts', :gift_path %></li> 
          <li><%= link_to 'My Account', :edit_current_host %></li>
  			  <% if logged_in? %>
  			    <li><%= link_to 'Logout', logout_path %></li>
  			  <% else %>
  			   <li><%= link_to 'Login', login_path %></li>
  			  <% end %>
  		  </ul>
      </div> 
      
      <% flash.each do |name, msg| %>
        <%= content_tag :div, msg, :id => "flash_#{name}" %>
      <% end %>
      <%= content_tag :h1, yield(:title) if show_title? %>
      <%= yield %>
  </div>  
  </body>
  
  <div id="footer">
    <%= link_to 'Contact Us', :contact_path %> | <%= link_to 'About Us', :about_path %> | <%= link_to 'Privacy Policy', :privacy_path %>
    <br />
    Property of Dominic Cerminara, Webmaster 
  </div>
</html>
